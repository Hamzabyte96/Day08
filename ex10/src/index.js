function mathTest(x, y) {
  if (x < 0 || y < 0) {
    return undefined;
  
    
  }
}

Math.round(Math.pow(Math.sqrt(2) + Math.sqrt(2), 2));
    console.log(Math.round(Math.pow(Math.sqrt(-2) + Math.sqrt(2), 2)))
    console.log(Math.pow(Math.sqrt(2) + Math.sqrt(-2), 2));
    return Math.round(Math.pow(Math.sqrt(2) + Math.sqrt(8), 2));
    return Math.round(Math.pow(Math.sqrt(3) + Math.sqrt(3), 2));
    return Math.round(Math.pow(Math.sqrt(0) + Math.sqrt(0), 2));
module.exports = mathTest;
